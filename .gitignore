# Claude-generated caches (regenerate on-demand; machine-specific)
cache/

# Local env override (creds belong in ~/.zshrc, not the repo)
.env
.env.local

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
node_modules/

# Refactor sanity-check snapshots (working dir; baseline is tracked).
tmp/after/
