# Claude-generated caches (regenerate on-demand; machine-specific)
cache/

# Local env override (creds belong in ~/.zshrc, not the repo)
.env
.env.local

# OS / editor noise
.DS_Store
*.swp
.idea/
.vscode/
node_modules/

# Working scratch (snapshot harness output, etc.). Anything we want
# tracked goes elsewhere.
tmp/
